# Legacy example retained for reference. The new app does not read .env files.
# Configure the model in Settings or use config.example.cfg as a starting point.
# Your private .env is never read, copied, or rewritten by the build.
llm=your-vision-model-id
url=http://localhost:11434/v1
key=
