# Copy beside klickwerk.exe as config.cfg, or let the app create it.
# TOML syntax. Model names remain case-sensitive.
version = 1
base_url = "http://localhost:11434/v1"
model = ""
# Use Settings to save a key protected with Windows DPAPI.
api_key = ""
screenshot_max_edge = 1280
request_timeout_seconds = 120
max_steps = 50
theme = "system"
reduce_motion = false
