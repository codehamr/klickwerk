# Project instructions

Always use English for project documentation, code comments, identifiers, UI text,
and build or log messages. Apply this convention to all future changes as well.
The application may still accept prompts and text input in other languages.
